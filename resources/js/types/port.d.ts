export type Port = {
    id: number;
    name: string;
    code: string;
    country: string;
    created_at: string;
    updated_at: string;
};

export type FormPort = {
    name: string;
    code: string;
    country: string;
};
